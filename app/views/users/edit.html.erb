<h3>User info</h3>

<% form_with model: @user,url:user_path(@user.id), method: :patch do |f| %>
  <p>Name</p>
  <%= f.text_field :title %>

  <p>Image</p>

  <p>Introduction</p>
  <%= f.text_area :body %>

  <%= f.submit 'Update User' %>

<% end %>